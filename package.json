{
  "name": "blogger-dashboard",
  "version": "1.0.0",
  "description": "Dashboard для блогера на Nuxt 3",
  "private": true,
  "scripts": {
    "build": "nuxt generate",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && gh-pages -d .output/public"
  },
  "dependencies": {
    "@nuxtjs/sitemap": "^7.4.7",
    "nuxt": "^3.8.4",
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.8.4",
    "gh-pages": "^6.3.0"
  }
}
